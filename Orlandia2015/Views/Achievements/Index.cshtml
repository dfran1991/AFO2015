@model IEnumerable<Orlandia2015.Models.Achievement>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.sName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.iSortOrder)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.sName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.iSortOrder)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.uAchievementID }) |
            @Html.ActionLink("Details", "Details", new { id=item.uAchievementID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.uAchievementID })
        </td>
    </tr>
}

</table>
